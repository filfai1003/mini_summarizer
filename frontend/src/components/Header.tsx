import React from 'react';

const Header: React.FC = () => {
	return (
		<header style={{ padding: '1rem', borderBottom: '1px solid #eaeaea' }}>
			<h2>Mini Summarizer</h2>
		</header>
	);
};

export default Header;
